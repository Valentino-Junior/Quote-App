import { DurationCountPipe } from './duration-count.pipe';

describe('DurationCountPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationCountPipe();
    expect(pipe).toBeTruthy();
  });
});
